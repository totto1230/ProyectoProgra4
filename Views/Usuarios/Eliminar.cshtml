@model ProyectoPrograCuatro.Models.Usuarios // Define el modelo para esta vista

@{
    ViewBag.Title = "Confirmar Eliminación Usuario"; // Establece el título de la página
}
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header bg-danger text-white">
                    <h2 class="mb-0">Eliminar Usuario</h2>
                </div>
                <div class="card-body">
                    @using (Html.BeginForm("Eliminar", "Usuarios", FormMethod.Post, new { @class = "form-horizontal" }))
                    {
                        <h4>Eliminar</h4>
                        <hr />
                        @Html.HiddenFor(model => model.Codigo)

                        <div class="form-group">
                            <div class="col-md-12 text-center">
                                <button type="submit" class="btn btn-danger">
                                    Eliminar
                                </button>
                                @ViewBag.Message
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>

    <div class="row mt-3">
        <div class="col-md-12 text-center">
            <a asp-action="Lista" class="btn btn-secondary">Regresar a Usuarios</a>
        </div>
    </div>
</div>
